server:
  port: ${PORT:8080}

nexon:
  api-key: ${NEXON_API_KEY:}
  api-url: ${NEXON_API_URL:https://open.api.nexon.com}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://127.0.0.1:3000}
